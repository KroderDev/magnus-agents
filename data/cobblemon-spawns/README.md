# Cobblemon spawn data

These JSON files come from `data/cobblemon/spawn_pool_world` in the official
Cobblemon Fabric 1.7.3 + Minecraft 1.21.1 JAR. Server datapacks may override
these rules, so replace or overlay this directory when the server pack changes.
